import { FormItem, FormLabel, FormDescription, FormMessage } from "src/components/ui/form";

export const FormFieldWrapper = ({
  name,
  label,
  description,
  children,
}: {
  name: string;
  label: string;
  description?: string;
  children: React.ReactNode;
}) => {
  return (
    <div>s</div>
  );
};

export default FormFieldWrapper;
